language: node_js
node_js:
- 6
install:
- yarn install
script:
- yarn lint
- yarn flow
deploy:
  provider: script
  skip_cleanup: true
  script:
  - yarn build && now --public --token $NOW_TOKEN build && now alias --token $NOW_TOKEN
  on:
    branch: master